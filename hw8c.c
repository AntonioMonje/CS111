#include <iostream>
using name space std;

int main()
{
  return 0;
}
int employees()
{
int employees
  cout << "Enter the number of employees: ";
  cin >> employees;
  if(employees < 1)
    {
      cout << "Invalid number!" << endl;
    }
  return employees;
}

int missed(int m)
{
  int days;
  int  total;
  int count;
  while(employees != 0)
    {
